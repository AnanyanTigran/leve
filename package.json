{
  "name": "leve",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:all": "concurrently \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/api\"",
    "build": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "lint": "npm run lint --workspace=apps/web && npm run lint --workspace=apps/api",
    "lint:fix": "npm run lint:fix --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/api",
    "prepare": "husky || true",
    "setup:s3-lifecycle": "tsx apps/api/scripts/setup-s3-lifecycle.ts"
  },
  "devDependencies": {
    "@types/sharp": "^0.31.1",
    "concurrently": "^8.2.2",
    "husky": "^9.1.7",
    "prisma": "^5.22.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.1051.0",
    "@prisma/client": "^5.22.0",
    "file-type": "^22.0.1",
    "lucide-react": "^1.16.0",
    "next-themes": "^0.4.6",
    "react": "^19.0.0"
  },
  "overrides": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
